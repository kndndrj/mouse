sensors = declare_dependency(
  sources: [
    'sensors/pmw3360.c',
  ],
)

buttons = declare_dependency(
  sources: [
    'button.c',
  ],
)

encoders = declare_dependency(
  sources: [
    'encoder.c',
  ],
)
deps += [sensors, buttons, encoders]
